{
  "name": "tuki_rules",
  "description": "Create an easily readable list of rules to validate before proceeding with the execution of the rest of the code.",
  "module": "src/index.js",
  "author": "kelvinauta",
  "main": "src/index.js",
  "scripts": {
    "dev": "bun --hot run src/example.js",
    "start": "bun run src/example.js"
  },
  "type": "module",
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5.0.0"
  },
  "dependencies": {
    "chalk": "^5.3.0"
  }
}
